#include <stdio.h>
int main (void)
{
    int i;
    //for loop -- known number of times
    for(int i = 0; i < 5; i++)
    {
        printf("Hello! ");
    }

    //while -- unknown number of times

    //do-while --unknown number, at least once
}